// Fix for hour labels not using panel-specific time interval
// Find these lines in script_new.js and make the following changes:

// In createSavedPanel function (around line 875-876), change:
hourLabel.textContent = formatHour(hour);

// To:
hourLabel.textContent = formatHour(hour, panelId);


// In addSchedulePanel function (around line 970-971), change:
hourLabel.textContent = formatHour(hour);

// To:
hourLabel.textContent = formatHour(hour, panelId);


// In updateAllPanelsTimeInterval function (around line 595-620), add these lines before the closing bracket:
        // Update hour labels to reflect the new interval
        const hourLabels = panelElement.querySelectorAll('.hour-label');
        hourLabels.forEach((label, index) => {
            label.textContent = formatHour(index, panelId);
        });
